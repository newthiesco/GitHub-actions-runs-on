name: Minute Cleanup
on:
  schedule:
    - cron: "*/1 * * * *"

jobs:
  Cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses:  actions/checkout@v4
      - name: cleanup task
        run:  echo "performing cleanup every minute"
